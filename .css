/* Body Background Color Gradient */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: linear-gradient(to right, #c6f0c2, #8ccf97); /* Light Green Gradient */
    color: #333;
    display: flex;
    flex-direction: row;
}

/* Side Navigation */
.sidenav {
    height: 100%;
    width: 250px;
    position: fixed;
    top: 0;
    left: 0;
    background: linear-gradient(to bottom, #006400, #228B22); /* Dark Green Gradient */
    padding-top: 20px;
    display: flex;
    flex-direction: column;
}

.sidenav a {
    padding: 15px 25px;
    text-decoration: none;
    font-size: 18px;
    color: white;
    display: block;
    transition: background-color 0.3s ease;
}

.sidenav a:hover {
    background-color: #555; /* Darker shade on hover */
}

/* Main Content Area */
.content {
    margin-left: 250px;
    padding: 20px;
    flex-grow: 1;
}

/* Section Style */
section {
    margin-bottom: 40px;
}

section h1 {
    font-size: 3rem; /* Bigger title size for emphasis on Home page */
    margin-bottom: 10px;
    color: #333;
}

section p {
    font-size: 1.2rem;
    color: #555;
}

/* Home Section Special Styling */
#home h1 {
    font-size: 4rem;
    font-weight: bold;
    color: #2d6a4f;
}

/* Mobile Responsiveness */
@media screen and (max-width: 768px) {
    .sidenav {
        width: 200px;
    }

    .content {
        margin-left: 200px;
    }

    section h1 {
        font-size: 2rem;
    }

    section p {
        font-size: 1rem;
    }
}

@media screen and (max-width: 480px) {
    .sidenav {
        width: 150px;
    }

    .content {
        margin-left: 150px;
    }
}
